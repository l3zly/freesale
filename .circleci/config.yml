version: 2.1
jobs:
  build:
    docker:
      - image: node:latest
      - image: mongo:latest
    steps:
      - checkout
      - run:
          name: Installing dependencies
          command: npm install
      - run:
          name: Running tests
          command: npm test
workflows:
  run-build:
    jobs:
      - build
